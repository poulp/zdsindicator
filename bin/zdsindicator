#!/usr/bin/env python
# -*- coding: utf8 -*-

import gobject
import gc
import gtk

from zdsindicator.zindicator import ZIndicator

if __name__ == "__main__":
    gobject.threads_init()
    if not gc.isenabled():
        gc.enable()
    z = ZIndicator()
    z.update()
    gtk.main()